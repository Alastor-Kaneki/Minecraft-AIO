name: Build and publish v0.1.3 alpha

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Build installable APK
        run: gradle :app:assembleDebug --no-daemon --stacktrace

      - name: Prepare release asset
        run: |
          mkdir -p release
          cp app/build/outputs/apk/debug/app-debug.apk release/Minecraft-AIO-v0.1.3-alpha.apk
          sha256sum release/Minecraft-AIO-v0.1.3-alpha.apk > release/Minecraft-AIO-v0.1.3-alpha.apk.sha256

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Minecraft-AIO-v0.1.3-alpha
          path: release/*

      - name: Create or repair GitHub prerelease
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: v0.1.3-alpha
        run: |
          TARGET="$(git rev-parse HEAD)"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" release/* --clobber
          else
            gh release create "$TAG" release/* \
              --target "$TARGET" \
              --title "Minecraft AIO v0.1.3-alpha" \
              --notes "Routes Vanilla Tweaks, Bedrock Tweaks, and BEComTweaks bundle builders into a dedicated in-app interactive picker. Supports JavaScript, uploads, normal downloads, generated blob downloads, secure TLS blocking, immersive mode, and saving generated packs directly to Downloads without an external browser." \
              --prerelease
          fi
